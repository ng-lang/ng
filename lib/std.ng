module std exports (
    printList
);

fun printList(list) {
    loop i = list {
        if (i.hasNext()) {
            val v = i.getNext();
            print(v.content);
            next v;
        }
    }
}
