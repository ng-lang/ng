val m = 1;

fun print x = x;

fun invoke () = (1, 2, 3, 4);

print (m);

val (a, b, c, d) = invoke();

val _ = a + 1;

val () = print(1);

val (x, y) = (a + b, b);

fun hello(x) = print(x);


fun fn a b [c, d, e] (f, g) [h, i...] = [a, b, c, d, e, f, g, h, sizeof(i)];

fun test_statement_body (x) {
    val y = x + 1;
    return x + y
}
